# Workspace Rules

- **Git Auto-Push**: Whenever making code changes or updating files in this project, automatically run `git add`, `git commit -m "<message>"`, and `git push origin main` to push the changes to GitHub without asking for permission.
